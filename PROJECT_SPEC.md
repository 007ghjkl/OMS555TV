# 基于 STM32 与 Qt 的电力监测终端自动化测试验证平台
## 项目需求规格说明（供 Codex / Agent 实施）

> 文档定位：本文件用于约束 Codex/Agent 从零实现项目。  
> 默认语言：中文。  
> 项目性质：面向校招“产品测试验证工程师”岗位的软硬件结合项目。  
> 核心关键词：STM32、C++、Qt、RS485、Modbus RTU、自动化测试、设备监控、测试报告、故障注入。

---

# 1. 项目背景

本项目模拟一个简化的电力设备状态监测终端，并开发配套的上位机监控与自动化测试软件。

项目目标不是实现工业级电力保护或真实高压测量，而是构建一个完整、可演示、可测试、可扩展的“设备端 + 工业通信 + 上位机 + 自动化测试”系统，用于体现以下能力：

1. STM32 基础嵌入式开发能力；
2. 传感器与模拟量采集能力；
3. RS485 / Modbus RTU 工业通信能力；
4. C++ / Qt 上位机开发能力；
5. 软件与硬件联调能力；
6. 自动化测试框架设计能力；
7. 功能、通信、边界、异常恢复、稳定性测试能力；
8. 测试日志、测试报告与缺陷定位能力。

项目优先保证完整性、可验证性与工程结构清晰，不追求无意义堆叠复杂技术。

---

# 2. 项目目标

最终系统由两部分组成：

- STM32 被测设备（DUT）
- C++ / Qt 上位机

二者通过 USB-RS485 / RS485 与 Modbus RTU 通信。

Qt 上位机同时承担：

- 实时监控
- 参数配置
- 通信调试
- 自动化测试
- 半自动测试
- 测试日志记录
- 测试报告生成

系统应支持两种主要工作模式：

## 2.1 运行监控模式

用于正常读取设备状态：

- 周期轮询设备；
- 显示实时数据；
- 显示通信状态；
- 显示设备告警；
- 绘制趋势曲线；
- 保存运行日志。

## 2.2 测试验证模式

用于执行测试：

- 暂停正常监控轮询；
- 测试引擎独占 Modbus 通信通道；
- 自动执行测试用例；
- 自动比较预期结果与实际结果；
- 记录原始报文、时间戳、响应时间；
- 生成测试结果；
- 导出测试报告。

原则上，运行监控与自动化测试不同时执行。

---

# 3. 非目标

以下内容不作为第一阶段强制要求：

1. 不要求接入真实高压、电流或市电设备；
2. 不要求实现 IEC 61850；
3. 不要求实现复杂电力保护算法；
4. 不要求实现 FreeRTOS；
5. 不要求实现 Linux 内核、驱动或 BSP；
6. 不要求加入树莓派中间层；
7. 不要求实现真实工业 HIL 系统；
8. 不要求加入数据库服务器、Web 后台或云平台；
9. 不要求过度设计分布式架构；
10. 不要求为了“技术栈丰富”而加入无实际作用的组件。

所有硬件实验必须使用安全低压环境。

---

# 4. 总体系统架构

```text
传感器 / 电位器 / 模拟输入
            │
            ▼
┌──────────────────────────────┐
│          STM32 DUT           │
│                              │
│  传感器采集                  │
│  模拟量采集                  │
│  数据处理                    │
│  阈值判断                    │
│  告警状态                    │
│  设备状态                    │
│  Modbus Slave                │
└──────────────┬───────────────┘
               │
        RS485 / Modbus RTU
               │
        USB-RS485 转换器
               │
┌──────────────▼───────────────┐
│        C++ / Qt 上位机        │
│                              │
│  实时监控                    │
│  参数配置                    │
│  通信调试                    │
│  自动化测试                  │
│  半自动测试                  │
│  原始报文日志                │
│  测试结果                    │
│  报告导出                    │
└──────────────────────────────┘
```

---

# 5. 技术选型

## 5.1 STM32

具体型号根据现有开发板决定，不在需求中绑定。

建议：

- STM32CubeMX / STM32CubeIDE；
- HAL 库；
- UART；
- GPIO；
- ADC；
- 可选 I2C；
- 独立或窗口看门狗可作为后续扩展。

第一阶段不强制 FreeRTOS，优先使用简单可靠的裸机事件循环。

## 5.2 通信

必须实现：

- RS485；
- Modbus RTU；
- STM32 作为 Modbus Slave；
- Qt 上位机作为 Modbus Master。

## 5.3 Qt 上位机

建议：

- C++17 或更新；
- Qt 6 优先，若环境限制可使用 Qt 5；
- CMake；
- Qt SerialPort；
- Qt Widgets 优先。

除非已有明确理由，不优先使用 QML。

## 5.4 测试用例配置

建议使用 JSON。

测试数据应尽量配置化，不应把全部测试步骤硬编码在 UI 代码中。

---

# 6. STM32 被测终端需求

## 6.1 数据采集

STM32 至少模拟或采集以下数据：

- A 相温度；
- B 相温度；
- C 相温度；
- 环境温度。

可选：

- 模拟电压；
- 模拟电流；
- 开关量状态；
- 设备内部温度；
- 其他已有传感器。

如果真实传感器不足，可使用：

- 电位器；
- 固定模拟值；
- 软件模拟值；
- ADC 输入。

项目重点不是测量精度，而是完整的数据链路与测试能力。

---

## 6.2 数据处理

设备端至少完成：

- 原始值转换；
- 工程量缩放；
- 上下限约束；
- 异常值识别；
- 阈值比较；
- 告警状态生成。

推荐温度使用整数缩放方式：

```text
实际温度 35.2 ℃
寄存器值 352
缩放系数 0.1
```

避免直接传输浮点数。

---

## 6.3 告警功能

至少支持：

- A 相温度高告警；
- B 相温度高告警；
- C 相温度高告警；
- 环境温度高告警。

阈值必须可以通过 Modbus 写入。

建议实现迟滞逻辑，例如：

```text
告警阈值：80.0℃
恢复阈值：75.0℃
```

避免在临界点频繁抖动。

---

## 6.4 设备状态

至少维护：

- 设备运行状态；
- 告警状态位；
- 通信错误计数；
- 系统运行时间；
- 固件版本号。

可选：

- 设备复位次数；
- 传感器故障位；
- 看门狗复位标志；
- 最近异常码。

---

# 7. Modbus RTU 需求

## 7.1 基本要求

必须实现并验证：

- CRC16；
- Slave 地址；
- 波特率配置；
- 数据位；
- 停止位；
- 奇偶校验。

推荐默认参数：

```text
Slave ID: 1
Baud Rate: 9600 或 115200
Data Bits: 8
Parity: None
Stop Bits: 1
```

具体值应可配置。

---

## 7.2 建议支持功能码

至少：

- 0x03 Read Holding Registers；
- 0x06 Write Single Register。

推荐扩展：

- 0x04 Read Input Registers；
- 0x10 Write Multiple Registers。

---

## 7.3 异常响应

必须支持标准异常情况，包括：

- 非法功能码；
- 非法寄存器地址；
- 非法数据值。

至少正确返回：

```text
0x01 Illegal Function
0x02 Illegal Data Address
0x03 Illegal Data Value
```

---

# 8. Modbus 寄存器设计原则

寄存器表必须单独维护文档。

建议示例：

| 类型 | 地址 | 名称 | 说明 |
|---|---:|---|---|
| Holding | 40001 | A 相温度 | ×0.1℃ |
| Holding | 40002 | B 相温度 | ×0.1℃ |
| Holding | 40003 | C 相温度 | ×0.1℃ |
| Holding | 40004 | 环境温度 | ×0.1℃ |
| Holding | 40010 | A 相告警阈值 | 可写 |
| Holding | 40011 | B 相告警阈值 | 可写 |
| Holding | 40012 | C 相告警阈值 | 可写 |
| Holding | 40013 | 环境温度阈值 | 可写 |
| Holding | 40020 | 告警状态字 | 位定义 |
| Holding | 40021 | 设备状态 | 枚举/位定义 |
| Holding | 40030 | 通信错误计数 | 只读 |
| Holding | 40031 | 运行时间低字 | 可拆 32 位 |
| Holding | 40032 | 运行时间高字 | 可拆 32 位 |
| Holding | 40040 | 固件主版本 | 只读 |
| Holding | 40041 | 固件次版本 | 只读 |

注意：

1. 在代码中明确区分逻辑寄存器编号与 Modbus PDU 中的 0-based 地址；
2. 文档必须明确说明 40001 对应协议地址 0；
3. 防止出现典型的 0-based / 1-based 偏移错误；
4. 多字节数据必须明确高低字顺序；
5. 缩放系数必须文档化。

---

# 9. Qt 上位机总体需求

Qt 程序应包含以下主要模块：

```text
MainWindow / Application
│
├── DeviceMonitor
├── DeviceConfig
├── CommunicationDebug
├── TestEngine
├── TestCaseLoader
├── TestResultManager
├── ReportGenerator
├── LogManager
└── ModbusMaster
```

UI 不得直接实现底层 Modbus 业务。

---

# 10. 通信抽象层需求

必须实现独立通信模块，例如：

```cpp
class IModbusClient;
class ModbusRtuClient;
```

或者：

```cpp
class ModbusMaster;
```

通信模块至少支持：

- 打开串口；
- 关闭串口；
- 配置串口；
- 读取寄存器；
- 写单寄存器；
- 超时检测；
- 错误返回；
- 原始 TX/RX 报文获取；
- 请求响应时间统计。

监控模块和测试模块必须复用同一通信抽象层。

禁止：

- 在多个 UI 页面分别直接操作 QSerialPort；
- 在监控代码和测试代码中各写一套 Modbus 协议；
- 将 CRC、解析、业务判断混在 UI 槽函数里。

---

# 11. 实时监控模块

## 11.1 功能

必须显示：

- 串口连接状态；
- 设备在线/离线；
- A/B/C 相温度；
- 环境温度；
- 告警状态；
- 固件版本；
- 通信统计。

可选：

- 温度趋势曲线；
- 最近告警列表；
- 通信失败计数；
- 当前轮询周期。

---

## 11.2 轮询

监控模式下进行周期轮询。

轮询周期建议可配置，例如：

```text
100 ms
500 ms
1000 ms
2000 ms
```

需要避免：

- 前一次请求未完成又发送下一次；
- UI 主线程阻塞；
- 同一串口并发请求。

---

# 12. 参数配置模块

允许读取和修改：

- 告警阈值；
- 可写配置寄存器；
- 可选设备地址；
- 可选通信参数。

修改操作需有：

- 输入范围校验；
- 写入结果提示；
- 写入后回读验证。

---

# 13. 通信调试模块

用于辅助定位 Modbus 问题。

至少显示：

- 时间戳；
- TX 原始字节；
- RX 原始字节；
- 功能码；
- 寄存器地址；
- 响应时间；
- CRC 校验结果；
- 错误说明。

建议显示格式：

```text
[12:03:11.320] TX 01 03 00 00 00 04 44 09
[12:03:11.347] RX 01 03 08 01 5E ...
RTT = 27 ms
```

---

# 14. 测试模式

进入测试模式时：

1. 停止实时监控轮询；
2. 等待已有请求完成；
3. 测试引擎取得通信通道控制权；
4. 执行测试；
5. 测试结束后释放通信通道；
6. 用户可选择恢复监控模式。

同一时刻只能有一个请求执行器控制 Modbus 通信。

---

# 15. 自动化测试引擎

## 15.1 基本能力

TestEngine 至少支持：

- 加载测试用例；
- 逐条执行；
- 单条执行；
- 批量执行；
- 跳过；
- 中止；
- 超时；
- 重试；
- 自动判定 PASS / FAIL；
- 保存实际值；
- 保存失败原因；
- 保存执行时间；
- 保存原始通信报文。

---

## 15.2 测试结果状态

至少支持：

```text
NOT_RUN
RUNNING
PASS
FAIL
SKIPPED
ERROR
```

---

# 16. 数据驱动测试

测试用例建议以 JSON 描述。

示例：

```json
{
  "id": "TC001",
  "name": "读取A相温度",
  "category": "function",
  "type": "read_register",
  "request": {
    "function": 3,
    "address": 0,
    "count": 1
  },
  "expected": {
    "type": "range",
    "min": -400,
    "max": 1250
  },
  "timeout_ms": 500
}
```

另一个示例：

```json
{
  "id": "TC010",
  "name": "非法寄存器地址",
  "category": "protocol",
  "type": "expect_exception",
  "request": {
    "function": 3,
    "address": 50000,
    "count": 1
  },
  "expected": {
    "exception_code": 2
  }
}
```

核心要求：

- 测试步骤与预期结果尽量配置化；
- 新增简单测试时不应修改 TestEngine 核心逻辑；
- 复杂测试可以通过专用测试处理器扩展。

---

# 17. 测试类型

至少实现以下测试类别。

## 17.1 功能测试

例如：

- 温度寄存器读取；
- 阈值读取；
- 阈值写入；
- 写后回读；
- 告警状态读取；
- 固件版本读取。

---

## 17.2 通信协议测试

例如：

- 正常读取；
- 正常写入；
- 非法寄存器地址；
- 非法数据值；
- 响应超时；
- CRC 错误统计；
- 多次连续请求。

对于无法直接通过标准 Master 生成的错误帧，可实现 Raw Frame 测试接口作为扩展。

---

## 17.3 边界测试

例如：

- 温度最小值；
- 温度最大值；
- 阈值最小值；
- 阈值最大值；
- 阈值等于当前值；
- 超范围写入；
- 寄存器边界地址。

---

## 17.4 数据一致性测试

验证：

- STM32 内部值；
- Modbus 寄存器值；
- Qt 解析后的工程值；

三者逻辑一致。

重点检查：

- 缩放；
- 正负数；
- 高低字；
- 地址偏移。

---

## 17.5 性能测试

统计：

- 请求响应时间；
- 平均响应时间；
- 最大响应时间；
- 最小响应时间；
- 请求总数；
- 成功数；
- 失败数；
- 超时率。

---

## 17.6 稳定性测试

至少支持配置：

```text
持续时间
轮询间隔
测试寄存器
允许失败率
```

目标可以逐步提高：

- 10 分钟；
- 1 小时；
- 8 小时；
- 24 小时。

测试结束后给出统计结果。

---

# 18. 半自动测试

对于需要人工操作硬件的测试，必须支持“半自动测试”。

典型场景：

- 拔掉 RS485；
- 重新插入 RS485；
- 按 STM32 Reset；
- 断开传感器；
- 人工改变传感器输入。

流程示例：

```text
TC201 通信中断恢复测试

步骤 1：
请断开 RS485 连接
[确认已断开]

系统检测：
连续 N 次超时
设备状态变为 Offline

步骤 2：
请重新连接 RS485
[确认已连接]

系统检测：
自动恢复通信

恢复时间：
2.8 秒

验收要求：
< 5 秒

结果：
PASS
```

半自动测试必须：

- 有明确人工提示；
- 有明确继续/取消操作；
- 由软件完成最终判定；
- 将人工步骤记录进报告。

---

# 19. 故障与异常注入

项目至少应覆盖以下故障场景：

1. RS485 断线；
2. 设备重启；
3. 非法寄存器地址；
4. 超范围参数；
5. 响应超时；
6. 传感器异常值；
7. 告警触发与恢复；
8. 可选 CRC 异常帧。

建议额外保留几个曾经故意制造并最终修复的问题，用于面试展示，例如：

- Modbus 地址偏移错误；
- 大小端解析错误；
- 断线后无法恢复；
- UI 轮询导致请求重叠。

但最终主分支应修复这些问题。

---

# 20. 日志系统

日志至少区分：

```text
INFO
WARN
ERROR
DEBUG
TEST
```

建议日志包含：

- 时间戳；
- 模块名；
- 事件；
- 请求 ID；
- 测试用例 ID；
- 错误码；
- 原始报文。

日志必须支持：

- UI 查看；
- 文件保存；
- 按日期或会话归档。

---

# 21. 测试报告

测试结束后必须生成正式测试报告。

第一阶段推荐生成：

- HTML；
- 可选 PDF。

报告至少包含：

## 21.1 基本信息

- 项目名称；
- 测试对象；
- 设备型号；
- 固件版本；
- 上位机版本；
- 测试人员；
- 测试时间；
- 通信参数；
- 测试环境。

## 21.2 汇总

- 总用例数；
- PASS；
- FAIL；
- ERROR；
- SKIPPED；
- 通过率；
- 总耗时。

## 21.3 明细

每条用例至少包含：

- ID；
- 名称；
- 类别；
- 前置条件；
- 操作步骤；
- 预期结果；
- 实际结果；
- 执行时间；
- PASS / FAIL；
- 失败原因。

## 21.4 附加信息

对失败项可附：

- TX；
- RX；
- 响应时间；
- 错误码；
- 相关日志。

---

# 22. UI 页面建议

建议至少包含：

```text
1. 设备监控
2. 参数配置
3. 通信调试
4. 自动化测试
5. 测试报告
6. 系统设置
```

自动化测试页建议布局：

```text
左侧：
测试套件 / 测试用例树

中间：
测试详情
当前步骤
预期结果
实际结果

右侧或底部：
实时执行日志
原始报文
统计信息
```

---

# 23. 线程与异步要求

Qt UI 主线程不得执行阻塞式长时间通信。

要求：

- 通信异步化；
- 或单独通信线程；
- 测试执行不可冻结 UI；
- 长时间稳定性测试时 UI 仍可响应；
- 可以中止测试；
- 禁止直接在工作线程操作 QWidget。

具体线程模型由实现阶段设计，但必须保证线程安全。

---

# 24. 状态管理

建议系统至少具有：

```text
DISCONNECTED
CONNECTED_IDLE
MONITORING
TESTING
STOPPING
ERROR
```

必须限制非法操作，例如：

- 未连接时禁止开始测试；
- 测试时禁止启动监控；
- 监控时进入测试必须先停止轮询；
- 串口打开后禁止随意修改部分通信参数。

---

# 25. 错误处理

不得仅使用 QMessageBox 代替完整错误机制。

错误至少分为：

- 串口错误；
- Modbus 超时；
- CRC 错误；
- 协议异常；
- 参数非法；
- 测试断言失败；
- 配置文件错误；
- 报告生成错误。

底层返回结构化错误，上层负责展示。

---

# 26. 非功能需求

## 26.1 可维护性

- 模块边界清晰；
- 避免大类；
- 避免 God Object；
- 业务逻辑不得堆积在 MainWindow。

## 26.2 可测试性

Modbus 业务逻辑应允许使用 Mock 或 Fake Client 测试。

例如：

```cpp
class IModbusClient {
public:
    virtual ReadResult readRegisters(...) = 0;
    virtual WriteResult writeRegister(...) = 0;
};
```

便于在没有真实 STM32 时测试 TestEngine。

## 26.3 可扩展性

后续应可以扩展：

- Modbus TCP；
- 更多传感器；
- 更多测试类型；
- Linux 版本；
- FreeRTOS 固件；
- 自动重连；
- Raw Frame 测试；
- CI 中的纯软件测试。

但这些不是第一阶段强制要求。

---

# 27. 推荐代码仓库结构

```text
project-root/
│
├── README.md
├── PROJECT_SPEC.md
├── AGENTS.md
│
├── docs/
│   ├── architecture.md
│   ├── modbus_register_map.md
│   ├── test_plan.md
│   ├── test_cases.md
│   └── bug_records.md
│
├── firmware/
│   └── stm32/
│       ├── Core/
│       ├── Drivers/
│       ├── App/
│       └── README.md
│
├── host/
│   └── qt/
│       ├── CMakeLists.txt
│       ├── src/
│       │   ├── app/
│       │   ├── communication/
│       │   ├── device/
│       │   ├── monitor/
│       │   ├── testing/
│       │   ├── report/
│       │   ├── logging/
│       │   └── ui/
│       ├── tests/
│       └── resources/
│
├── testcases/
│   ├── functional/
│   ├── protocol/
│   ├── boundary/
│   ├── recovery/
│   └── stability/
│
└── output/
    ├── logs/
    └── reports/
```

目录可根据实际工程调整，但必须保持 firmware 与 host 分离。

---

# 28. 开发阶段

Codex 不应一次性实现全部功能。

按以下阶段递进开发。

## Phase 0：工程骨架

完成：

- 仓库目录；
- Qt 工程可编译；
- STM32 工程可编译；
- 文档目录；
- 基础 CMake；
- 基础日志。

验收：

- 两端项目均可独立构建。

---

## Phase 1：STM32 基础采集

完成：

- UART；
- ADC / 传感器；
- 基本数据结构；
- 模拟温度值；
- 简单调试输出。

验收：

- 可以稳定获得数据。

---

## Phase 2：Modbus RTU Slave

完成：

- 0x03；
- 0x06；
- CRC；
- 寄存器映射；
- 异常响应。

验收：

- 可通过第三方 Modbus Master 软件正确访问。

---

## Phase 3：Qt Modbus 通信

完成：

- 串口连接；
- 读寄存器；
- 写寄存器；
- 超时；
- TX/RX 日志。

验收：

- Qt 可以稳定读取 STM32 数据。

---

## Phase 4：实时监控

完成：

- 周期轮询；
- 实时数据显示；
- 状态显示；
- 告警显示；
- 可选曲线。

验收：

- 连续运行至少 30 分钟无明显异常。

---

## Phase 5：TestEngine

完成：

- 测试用例数据模型；
- JSON 加载；
- 测试执行；
- PASS / FAIL；
- 超时；
- 中止；
- 执行日志。

验收：

- 至少可以执行 5 个自动化测试。

---

## Phase 6：完整测试套件

完成：

- 功能；
- 协议；
- 边界；
- 数据一致性；
- 恢复；
- 稳定性。

验收：

- 至少 20 条有效测试用例。

---

## Phase 7：半自动测试

完成：

- 人工操作提示；
- 确认机制；
- 自动恢复检测；
- 自动判定。

验收：

- RS485 断线恢复测试可完整执行。

---

## Phase 8：测试报告

完成：

- 测试汇总；
- 用例明细；
- 失败原因；
- 原始通信数据；
- HTML 导出；
- 可选 PDF。

验收：

- 一键生成完整报告。

---

## Phase 9：工程完善

完成：

- README；
- 架构图；
- 寄存器表；
- 测试计划；
- Bug 记录；
- 截图；
- 示例报告。

最终目标：

项目可以直接用于简历和面试展示。

---

# 29. 最低验收标准

整个项目最终至少应达到：

1. STM32 可稳定采集或模拟 4 路监测数据；
2. STM32 实现 Modbus RTU Slave；
3. Qt 实现 Modbus RTU Master；
4. Qt 能实时监控设备；
5. Qt 能配置阈值；
6. Qt 能显示告警；
7. Qt 能查看 TX/RX；
8. Qt 有独立测试模式；
9. 支持 JSON 测试用例；
10. 至少 20 条有效测试；
11. 至少包含功能、协议、边界、异常、稳定性测试；
12. 至少实现 1 个半自动故障恢复测试；
13. 能统计响应时间和通信成功率；
14. 能生成测试报告；
15. 有完整 README 和技术文档；
16. 可进行现场演示。

---

# 30. 推荐进阶验收标准

时间充足时增加：

1. 30~50 条测试用例；
2. 8 小时稳定性测试；
3. 自动重连；
4. Raw Modbus 帧发送；
5. CRC 错误注入；
6. Mock Modbus Client；
7. TestEngine 单元测试；
8. Qt 跨 Windows / Linux 构建；
9. STM32 看门狗；
10. FreeRTOS 版本；
11. 更丰富的传感器故障模拟。

这些属于加分项，不应阻塞 MVP。

---

# 31. 文档要求

项目必须维护：

```text
README.md
PROJECT_SPEC.md
docs/architecture.md
docs/modbus_register_map.md
docs/test_plan.md
docs/test_cases.md
docs/bug_records.md
```

所有项目文档默认使用中文。

关键技术名词可以保留英文。

---

# 32. README 最终应展示

README 至少包含：

- 项目简介；
- 项目截图；
- 系统架构；
- 硬件列表；
- 软件环境；
- 构建方法；
- 使用方法；
- Modbus 简介；
- 自动化测试说明；
- 测试结果示例；
- 项目亮点；
- 后续规划。

---

# 33. Codex / Agent 执行约束

以下要求为强制约束。

## 33.1 工作原则

Codex 必须：

1. 先阅读本文件；
2. 在编码前理解当前仓库结构；
3. 不随意重构已稳定工作的模块；
4. 每次只完成一个明确阶段；
5. 每个阶段结束后执行验证；
6. 发现需求冲突时先记录，不擅自扩大需求；
7. 优先实现简单、清晰、可验证的方案；
8. 不为炫技引入无必要框架。

---

## 33.2 输出语言

Codex：

- 对用户的说明使用中文；
- 生成的项目文档使用中文；
- Commit 信息建议使用中文或统一英文；
- 代码标识符使用英文；
- 技术标准名词保持原文。

---

## 33.3 编码原则

必须：

- 使用明确的模块边界；
- 避免重复代码；
- 避免将协议代码写在 UI 中；
- 避免长函数；
- 避免大规模全局变量；
- 明确错误处理；
- 对关键状态使用 enum；
- 对数据模型使用结构体或类；
- 对公共接口写必要注释。

禁止：

- 为简单逻辑引入复杂设计模式；
- 在没有测试的情况下做大规模重构；
- 无理由添加外部依赖；
- 用 sleep 解决所有异步问题；
- 在 Qt UI 主线程执行长阻塞任务。

---

## 33.4 修改流程

每次任务建议遵守：

```text
1. 阅读相关代码和文档
2. 输出简短实施计划
3. 修改最小必要范围
4. 构建
5. 运行已有测试
6. 新增对应测试
7. 更新相关文档
8. 汇报修改内容和验证结果
```

---

## 33.5 禁止虚构结果

Codex 不得虚构：

- 测试通过率；
- 性能数据；
- 响应时间；
- 连续运行时长；
- Bug 数量；
- 硬件测试结果。

无法在当前环境验证时，必须明确写：

```text
未在真实硬件环境验证
```

而不是假设测试已经通过。

---

# 34. 项目简历导向

实现过程中应优先产生可量化成果。

最终应能真实得到类似数据：

```text
测试用例数量：XX
自动测试数量：XX
半自动测试数量：XX
连续稳定运行：XX 小时
Modbus 请求数量：XX
平均响应时间：XX ms
最大响应时间：XX ms
通信失败率：XX %
发现并修复问题：XX 项
```

这些值必须来自真实测试。

---

# 35. 最终项目定位

本项目最终不是：

> 一个普通的 STM32 温度采集系统。

而应体现为：

> 一个针对嵌入式电力监测终端的软硬件联合自动化测试验证平台。

重点能力排序：

```text
自动化测试与验证
    >
工业通信与联调
    >
C++ / Qt 工具开发
    >
STM32 设备端实现
    >
传感器本身的复杂程度
```

Codex 在后续实现过程中应始终遵守这一优先级，避免把项目逐渐做成纯嵌入式课程设计。
